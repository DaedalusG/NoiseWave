<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        script( async defer src="js/renderWaveform.js")
        script(  src="js/audioPlayer.js")
    body
        h2= songData.title
        h2= songData.User.username
        p= url

        #player
            //- a#player__control.paused(href=''): img#play-pause(src="images/play.png")
            input#controls(type="image", src="images/play.png")
            audio#  (src=url)
            
            p#current-time
            p#duration


            //- canvas goes here